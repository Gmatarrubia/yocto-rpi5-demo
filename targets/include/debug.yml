header:
  version: 18

local_conf_header:
  debug_features: |
    EXTRA_IMAGE_FEATURES += "debug-tweaks"
    CORE_IMAGE_EXTRA_INSTALL += " socat \
       git \
       python3-pip \
    "
    OVERRIDES:append = ":debug"
